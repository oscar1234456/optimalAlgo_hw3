function [newCubs] = mutation(oldCubs, pm)
arguments
     oldCubs
     pm
end

for now=1:length(oldCubs)
    GOD = rand(1,48);
    for i=1:length(GOD)
        if(GOD(i)<=pm)
            
        end
    end
end



return